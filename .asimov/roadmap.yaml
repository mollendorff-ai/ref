# Möllendorff Asimov Roadmap
#
# WHAT to build - milestones only
#
# See: CHANGELOG.md for release history
# See: docs/adr/ for detailed rationale

current:
  version: "1.3.0"
  status: in_progress
  summary: "Open source readiness"
  goal: "FOSS_READY"
  deliverables:
    - "[x] MIT license"
    - "[x] README rewrite (token optimization value prop, output example)"
    - "[x] Clippy pedantic zero warnings, zero bypasses"
    - "[x] Test strategy ADR-003 (unit vs e2e, #[ignore] for Chrome tests)"
    - "[ ] Re-enable GitHub Actions (CI, multi-platform release, crates.io publish)"

next:
  - version: "1.4.0"
    summary: "Reference management"
    goal: "REF_MANAGEMENT"
    deliverables:
      - "[ ] Stale reference detection (not cited anywhere)"
      - "[ ] Reference deduplication"

backlog:
  - summary: "MCP Server Mode"
    goal: "MCP_SERVER"
    adr: "docs/adr/ADR-001-MCP-SERVER-MODE.md"
    note: "Deferred - no iOS support, CLI via Bash works fine"
  - "Proxy support (--proxy)"
  - "Export to markdown report"
  - "Image/OCR extraction from PDFs"
