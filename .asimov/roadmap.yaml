# Möllendorff Asimov Roadmap
#
# WHAT to build - milestones only
#
# Migration: Node.js/Puppeteer → Rust/chromiumoxide
# Goal: Single static binary, no node_modules, UPX compressed
#
# See: docs/PROTOCOL_GOALS.md for core values
# See: CHANGELOG.md for release history
# See: docs/adr/ for detailed rationale

current:
  version: "1.2.0"
  status: complete
  summary: "Enhanced extraction & SPA support"
  goal: "BETTER_EXTRACTION"
  deliverables:
    - "[x] PDF table extraction (whitespace column detection, markdown output)"
    - "[x] PDF heading detection improvements (numbered, Roman, academic, legal)"
    - "[x] Network idle wait for SPA support (ADR-002)"

next:
  - version: "1.3.0"
    summary: "Reference management"
    goal: "REF_MANAGEMENT"
    deliverables:
      - "[ ] Stale reference detection (not cited anywhere)"
      - "[ ] Reference deduplication"

backlog:
  - summary: "MCP Server Mode"
    goal: "MCP_SERVER"
    adr: "docs/adr/ADR-001-MCP-SERVER-MODE.md"
    note: "Deferred - no iOS support, CLI via Bash works fine"
  - "Proxy support (--proxy)"
  - "Export to markdown report"
  - "Image/OCR extraction from PDFs"
